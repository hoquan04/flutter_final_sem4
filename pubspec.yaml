name: flutter_final_sem4
description: "A Flutter project about fruit"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.0
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.9.0
  http: ^1.2.2
  nb_utils: ^7.1.8
  html: ^0.15.4
  webview_flutter: ^4.2.2
  image_picker: ^1.1.2
  pull_to_refresh: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  timeago: ^3.6.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/grocery/
